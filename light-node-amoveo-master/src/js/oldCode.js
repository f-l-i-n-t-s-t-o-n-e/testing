
//                offers.style.textIndent = "50px";

/*
            for(let i = 0; i < lengthVar; i++){

                if (i < sortedArray.length){

                    console.log("in truvdiv")

                    trueDiv.innerHTML = " Risk: " + sortedArray[i][3] + " | Profit: " + sortedArray[i][4] + " | Odds: " + sortedArray[i][1];

                }else{

                    trueDiv.innerHTML = " Risk: --- | Profit: --- | Odds: ---";

                }

                if (i < sortedArray2.length){

                    falseDiv.innerHTML = " Odds: " + sortedArray2[i][1] + " | Profit: " + sortedArray2[i][4] + " | Risk: " + sortedArray2[i][3] + " ";

                }else{

                    falseDiv.innerHTML = " Odds: --- | Profit: --- | Risk: --- ";

                }

                if (i < sortedArray.length){

                swapOfferTrue = sortedArray[i][2];

                }else{

                swapOfferTrue = 0;

                }

                if (i < sortedArray2.length){

                swapOfferFalse = sortedArray2[i][2];

                }else{

                swapOfferFalse = 0;

                }

                console.log("i and truediv: " + i);
                console.log("i and truediv: " + trueDiv.innerHTML);


        var buttonTrue = button_maker2("Accept", function() { viewTrading(swapOfferTrue) });

        var buttonFalse = button_maker2("Accept trade", function() { viewTrading(swapOfferFalse) });

//            buttonTrue.style.display = 'inline';
        console.log("truediv is xx: " + trueDiv.innerHTML);

   //         offers.appendChild(br());

            offers.appendChild(holderDiv);
            holderDiv.appendChild(br());


            holderDiv.appendChild(buttonTrue);

//            buttonTrue.style.padding = "0px";            
//            buttonTrue.appendChild(br());
            holderDiv.appendChild(text(trueDiv.innerHTML));
//            firstLine2.appendChild(trueDiv);
            holderDiv.appendChild(text(" |_____| "));

            holderDiv.appendChild(text(falseDiv.innerHTML));
            holderDiv.appendChild(buttonFalse);


//            trueDiv.style.display = 'inline';


  //          buttonTrue.appendChild(text(" | "));
  //          buttonTrue.appendChild(falseDiv);
   //                    falseDiv.style.display = 'inline';
   //         buttonTrue.appendChild(buttonFalse);
    //        buttonFalse.style.display = 'inline';

    //        buttonFalse.appendChild(br());
     //       offers.appendChild(br());
     //       offers.appendChild(br());

            }



            */

//                var button = button_maker2("Accept trade", function() { viewTrading(swapOffer2) });
            